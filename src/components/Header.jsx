import React from 'react';

export default function Header() {
  return (
    <header id="header" className="header container-fluid">
      <div id="app-title" className="header-row row">
        <h1 className="header-title">Daily Weather</h1>
      </div>
    </header>
  );
}
